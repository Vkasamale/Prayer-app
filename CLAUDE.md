# Working on the Prayer App

The standing rules live in [AGENTS.md](AGENTS.md). Read it before doing anything
else. It is the same file other agent tools look for; this one exists because
Claude Code loads `CLAUDE.md` automatically and not `AGENTS.md`.
